import Foundation
import VikiClowKit

// Prefer the VikiClowKit wrapper to keep gateway request payloads consistent.
typealias AnyCodable = VikiClowKit.AnyCodable
typealias InstanceIdentity = VikiClowKit.InstanceIdentity
